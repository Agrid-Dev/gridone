{
  "additionalProperties": false,
  "properties": {
    "gateway_ip": {
      "description": "KNX/IP gateway host or IP (no protocol prefix)",
      "minLength": 1,
      "pattern": "^[^\\s:/]+(\\.[^\\s:/]+)*$",
      "title": "Gateway Ip",
      "type": "string"
    },
    "port": {
      "default": 3671,
      "exclusiveMinimum": 0,
      "title": "Port",
      "type": "integer"
    },
    "tunneling_mode": {
      "default": "udp",
      "enum": ["udp", "tcp"],
      "title": "Tunneling Mode",
      "type": "string"
    },
    "secure_device_authentication_password": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "KNX IP-Secure device authentication password (set together with the secure user password)",
      "secret": true,
      "title": "Secure Device Authentication Password"
    },
    "secure_user_password": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "KNX IP-Secure user password (set together with the secure device authentication password)",
      "secret": true,
      "title": "Secure User Password"
    },
    "secure_user_id": {
      "default": 2,
      "description": "KNX IP-Secure tunnel user ID",
      "exclusiveMinimum": 0,
      "title": "Secure User Id",
      "type": "integer"
    }
  },
  "required": ["gateway_ip"],
  "title": "KNXTransportConfig",
  "type": "object"
}
