/** Camel-cased view of the `liquid_detector` standard attribute schema
 *  (packages/devices_manager .../standard_schemas/registry/liquid_detector.py). */
export type LiquidDetectorValues = {
  /** Conductive liquid bridging the probe. */
  liquidDetected?: boolean | null;
};
