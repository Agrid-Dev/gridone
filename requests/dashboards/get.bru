meta {
  name: get dashboard
  type: http
  seq: 4
}

get {
  url: {{BASE_URL}}/dashboards/:dashboard_id
  auth: inherit
}

params:path {
  dashboard_id: {{dashboard_id}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{dashboard_id}}
}
