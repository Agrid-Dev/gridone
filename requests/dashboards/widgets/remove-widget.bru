meta {
  name: remove widget
  type: http
  seq: 4
}

delete {
  url: {{BASE_URL}}/dashboards/:dashboard_id/widgets/:widget_id
  body: none
  auth: inherit
}

params:path {
  dashboard_id: {{dashboard_id}}
  widget_id: {{widget_id}}
}

assert {
  res.status: eq 204
}
