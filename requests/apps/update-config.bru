meta {
  name: update app config
  type: http
  seq: 5
}

patch {
  url: {{BASE_URL}}/apps/:app_id/config
  body: json
  auth: inherit
}

params:path {
  app_id: abc123
}

body:json {
  {
    "lat": 48.8566,
    "lng": 2.3522
  }
}

assert {
  res.status: eq 200
  res.body.push_status: isDefined
}
