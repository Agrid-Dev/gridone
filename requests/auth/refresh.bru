meta {
  name: refresh
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/auth/token
  body: formUrlEncoded
  auth: none
}

body:form-urlencoded {
  grant_type: refresh_token
  refresh_token: {{REFRESH_TOKEN}}
}
