[project]
name = "gridone-commands"
version = "0.1.0"
description = "Command dispatch and lifecycle management for device write operations"
readme = "README.md"
authors = []
requires-python = ">=3.13"
dependencies = [
    "gridone-models",
    "asyncpg>=0.30.0",
    "pydantic>=2.0",
]

[build-system]
requires = ["uv_build>=0.9.7,<0.11.0"]
build-backend = "uv_build"

[tool.pytest.ini_options]
pythonpath = ["src", "../models/src", "tests"]
markers = [
    "integration: mark a test as an integration test",
]

[tool.uv.build-backend]
module-name = "commands"
module-root = "src"
