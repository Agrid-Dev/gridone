[project]
name = "gridone-docs"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
]

[tool.uv]
# Standalone project — not part of the monorepo workspace
